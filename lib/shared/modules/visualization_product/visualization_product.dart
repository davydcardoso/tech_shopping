import 'package:flutter/material.dart';

class VisualizationProductPage extends StatelessWidget {
  const VisualizationProductPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
